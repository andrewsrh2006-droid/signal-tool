# Core dependencies for signal_tool
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
openpyxl>=3.1

# For the Streamlit web app (optional but recommended)
streamlit>=1.32
plotly>=5.18
tabulate>=0.9        # for markdown table rendering in reports
